anthropic>=0.40
openai>=1.30
google-genai>=1.0
# >=1.0.1: 1.0.0 couldn't read pre-1.0 pairtree caches; 1.0.1 adds
# legacy_read + a migration tool. data/stash was migrated to the 1.0
# layout on 2026-07-07 (34 stashes, 149,288 entries, verified against
# 0.4 ground truth; old *.lz4+b64 dirs retained as rollback copies).
hashstash[rec]>=1.0.1
pydantic>=2.0
json-repair>=0.25
tqdm>=4.64
pandas>=1.5
