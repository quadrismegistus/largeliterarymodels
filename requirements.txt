anthropic>=0.40
openai>=1.30
google-genai>=1.0
# <1.0: hashstash 1.0.0 cannot read pre-1.0 pairtree caches (len() counts
# entries and keys() decodes, but getitem/contains/items() all miss — even
# with b64=True), which would orphan data/stash. Unpin once upstream ships
# legacy-read support or a migration tool, then migrate deliberately.
hashstash[rec]>=0.4.0,<1.0
pydantic>=2.0
json-repair>=0.25
tqdm>=4.64
pandas>=1.5
